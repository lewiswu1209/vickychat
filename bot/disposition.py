
from enum import Enum

class Disposition(Enum):
    """
    Enum for the disposition of the bot.
    """
    COLD = "cold.json"
    ECCENTRIC = "eccentric.json"
    ELEGANT = "elegant.json"
    NORMAL = "normal.json"
